{
  "name": "amp-service",
  "profile": "apex",
  "hooks": {
    "build": "rm -Rf node_modules && PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=1 npm install --production && make"
  },
  "proxy": {
    "timeout": 25,
    "command": "./node-v8.9.4-linux-x64/bin/node index.js"
  },
  "lambda": {
    "memory": 3008
  }
}
